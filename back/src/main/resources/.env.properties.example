# Informations de la base de données à modifier
DB_USER=user_base
DB_PASSWORD=password_base
DB_NAME=nom_de_la_base
DB_HOST=host

# Liste des domaines autorisé séparé par des ","
DOMAINS=liste_des_domaines_autorises

# Cle RSA 2048 bits, generee avec :
#   openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out private.pem
#   openssl rsa -in private.pem -pubout -out public.pem
# puis copiees ici sans les lignes -----BEGIN/END----- et sans retour a la ligne :
#   grep -v -- '-----' private.pem | tr -d '\n'
#   grep -v -- '-----' public.pem | tr -d '\n'
RSA_PRIVATE_KEY=
RSA_PUBLIC_KEY=